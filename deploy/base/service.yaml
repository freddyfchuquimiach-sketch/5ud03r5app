# deploy/base/service.yaml  ← VERSIÓN FINAL QUE FUNCIONA
apiVersion: v1
kind: Service
metadata:
  name: app-service-fixed          # ← nombre que NO recibe suffix
  labels:
    app: 5ud03r5app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: 5ud03r5app
    version: blue                  # tráfico inicial