# deploy/base/kustomization.yaml   ? NUEVO ARCHIVO (OBLIGATORIO)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml   # si lo tienes