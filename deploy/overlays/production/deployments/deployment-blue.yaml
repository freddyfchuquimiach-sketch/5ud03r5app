apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-blue
  labels:
    app: 5ud03r5app
    version: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 5ud03r5app
      version: blue
  template:
    metadata:
      labels:
        app: 5ud03r5app
        version: blue
    spec:
      containers:
      - name: app
        image: freddych/5ud03r5app:latest  # Este tag se reemplaza con el script
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
