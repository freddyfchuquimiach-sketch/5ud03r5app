apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: 5ud03r5app
    version: blue   # Cambia a green cuando estés listo
